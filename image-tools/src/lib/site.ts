/** Working title — the project is unnamed so far. Swap both when christened. */
export const SITE_NAME = 'image-tools';

/** TODO: set the real domain before launch — canonicals and the sitemap use it. */
export const SITE_URL = 'https://image-tools.example';
