// Static output: there is no server side to this, the diagram never leaves the
// tab, and prerendering means the page is on screen before anything else runs.
export const prerender = true;
export const ssr = true;
