{
  "name": "checkpost",
  "version": "0.1.0",
  "private": true,
  "description": "Shared checklists that live at a link. No accounts.",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:contract": "pnpm --filter @checkpost/contract build",
    "dev": "pnpm run build:contract && pnpm --parallel --filter @checkpost/contract --filter @checkpost/api --filter @checkpost/web dev",
    "dev:api": "pnpm run build:contract && pnpm --parallel --filter @checkpost/contract --filter @checkpost/api dev",
    "dev:web": "pnpm run build:contract && pnpm --parallel --filter @checkpost/contract --filter @checkpost/web dev",
    "dev:app": "cd apps/app && flutter run",
    "build": "pnpm run build:contract && pnpm --filter @checkpost/api build && pnpm --filter @checkpost/web build",
    "test": "pnpm run build:contract && pnpm --filter @checkpost/api test",
    "typecheck": "pnpm run build:contract && pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "db:up": "docker compose up -d db",
    "db:push": "pnpm --filter @checkpost/api db:push",
    "db:generate": "pnpm --filter @checkpost/api db:generate",
    "db:migrate": "pnpm --filter @checkpost/api db:migrate",
    "app:analyze": "cd apps/app && flutter analyze",
    "app:test": "cd apps/app && flutter test"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
