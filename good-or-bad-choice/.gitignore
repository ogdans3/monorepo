node_modules/
dist/
*.tsbuildinfo

.env
.env.*
!.env.example

# Flutter / Dart
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/build/
app/.packages

# The browser bundle, staged into the API for a local run. Built, not kept.
api/public/
